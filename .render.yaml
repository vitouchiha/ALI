services:
  - type: web
    name: aliexpress-affiliate-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        value: TUO_TOKEN_DEL_BOT
